extends layout

block content
  div.container
    div.row

      div.col-md-12.main-content
        //#{carer.Patients.Billy.fname}
        //h2 Monitor patients of Carer #{carer.Patients.Billy.fname}
        div.row
        for patient in patients
          div.col-sm-12
            .card
              h3.card-header Name: #{patient.fname} #{patient.lname}
              .card-block
                h4.card-title Condition: #{patient.condition}
                p.card-text #{patient.age} years old
                p.card-text
                  a(type="tel" href="tel:" + patient.contactNo) #{patient.contactNo}
                a.btn.btn-primary(href='#') View location


              iframe(width="100%", height="400", frameborder="0", id="map" + index,
              style="border:0; display:block;"
              src='https://www.google.com/maps/embed/v1/place?' +
              'key=AIzaSyDqIZZQknynh-WN6s5JNkyDG_HfVMY_cBU&q=' +
              patient.long + ',' + patient.lat)

            //button(
            //onclick="document.getElementById('map" + index + "').display = 'none'"
            //) View location of #{index}